spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meng_admin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: aaa123..

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 3000ms

logging:
  level:
    me.dqq.admin: debug
    com.baomidou.mybatisplus: debug
