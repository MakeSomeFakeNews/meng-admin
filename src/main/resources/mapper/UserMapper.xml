<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.dqq.admin.module.user.mapper.UserMapper">

    <!-- 分页查询用户列表 -->
    <select id="selectUserPage" resultType="me.dqq.admin.module.user.entity.SysUser">
        SELECT id, username, nickname, avatar, email, phone, status, create_by, update_by, create_time, update_time
        FROM sys_user
        WHERE deleted = 0
        <if test="query.username != null and query.username != ''">
            AND username LIKE CONCAT('%', #{query.username}, '%')
        </if>
        <if test="query.nickname != null and query.nickname != ''">
            AND nickname LIKE CONCAT('%', #{query.nickname}, '%')
        </if>
        <if test="query.status != null">
            AND status = #{query.status}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 批量插入用户角色关联 -->
    <insert id="insertUserRoles">
        INSERT INTO sys_user_role (user_id, role_id) VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

</mapper>
